MONGODB_URL=mongodb://localhost:27017
MONGODB_DB=amplify
FRONTEND_ORIGIN=http://localhost:5173
# Both from tone3000.com → Settings → API Keys
TONE3000_KEY=t3k_pub_...     # publishable key — client_id in OAuth, same as frontend
TONE3000_SECRET=t3k_cs_...   # client secret — only used in token exchange
