# Publishable key — safe to expose in the browser bundle
# Get from tone3000.com → Settings → API Keys
VITE_TONE3000_KEY=t3k_pub_...

# Our backend URL (change for production)
VITE_API_URL=http://localhost:8000
